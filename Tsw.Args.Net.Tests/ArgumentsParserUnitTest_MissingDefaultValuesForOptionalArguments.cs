using Tsw.Args.Net.Parser;
using Tsw.Args.Net.Tests.Arguments;

namespace Tsw.Args.Net.Tests
{
    public class ArgumentsParserUnitTest_MissingDefaultValuesForOptionalArguments
    {
        [Fact]
        public void TestArgumentParsing()
        {
#warning TODO: likely to remove this requirement
            //var result = Utils.GetParser(types: [typeof(MissingDefaultValuesForOptionalArguments)])
            //    .Run<MissingDefaultValuesForOptionalArguments>(Utils.ToArgs("required optional"), (arguments) => 0);

            //Assert.Equal(ParseResult.Error, result);
        }
    }
}
